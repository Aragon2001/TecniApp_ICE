<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:paddingStart="36dp"
    android:paddingEnd="36dp"
    android:paddingBottom="24dp">

    <!-- Contenedor circular del ícono con color de acento -->
    <FrameLayout
        android:id="@+id/illustrationBg"
        android:layout_width="192dp"
        android:layout_height="192dp">

        <ImageView
            android:id="@+id/imageIllustration"
            android:layout_width="88dp"
            android:layout_height="88dp"
            android:layout_gravity="center"
            android:contentDescription="@string/onboarding_illustration_content_description"
            android:importantForAccessibility="no"
            tools:src="@drawable/ic_menu_home" />

    </FrameLayout>

    <!-- Título de la página -->
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/textTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:gravity="center"
        android:textAppearance="@style/TextAppearance.Material3.HeadlineSmall"
        android:textStyle="bold"
        android:textColor="?android:textColorPrimary"
        tools:text="Bienvenido a TecniApp ICE" />

    <!-- Descripción -->
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/textDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:lineSpacingExtra="6dp"
        android:textAppearance="@style/TextAppearance.Material3.BodyLarge"
        android:textColor="?android:textColorSecondary"
        tools:text="Centraliza tus operaciones de campo con autenticación segura y accesos rápidos a los módulos clave." />

</LinearLayout>
