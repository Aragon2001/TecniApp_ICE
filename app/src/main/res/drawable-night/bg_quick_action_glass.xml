<?xml version="1.0" encoding="utf-8"?>
<!-- Fondo oscuro para botones de acceso rápido en modo nocturno -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Capa base: superficie oscura semi-transparente -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="270"
                android:startColor="#CC1E2630"
                android:endColor="#E61A2230"
                android:type="linear" />
            <corners android:radius="18dp" />
        </shape>
    </item>

    <!-- Borde sutil blanco (15% opacidad) para separar del fondo -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="18dp" />
            <stroke
                android:width="1dp"
                android:color="#26FFFFFF" />
        </shape>
    </item>

</layer-list>
