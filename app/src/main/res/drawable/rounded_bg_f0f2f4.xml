<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color de fondo -->
    <solid android:color="#F0F2F4"/>

    <!-- Bordes redondeados -->
    <corners android:radius="12dp"/>

    <!-- Borde exterior (opcional) -->
    <stroke
        android:width="1dp"
        android:color="#E0E0E0"/>

    <!-- Padding para el espacio adicional -->
    <padding
        android:left="8dp"
        android:right="32dp"
        android:top="8dp"
        android:bottom="8dp"/>
</shape>
