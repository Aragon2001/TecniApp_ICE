*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# ignore generated command outputs
Commit
Push
Manage

# Proyecto web hermano: NO se versiona en este repo Android (AUDITORIA.md §A7).
# Debe tener su propio repositorio Git. Se ignora la carpeta completa.
Tecniapp web/
Tecniapp web/node_modules/
Tecniapp web/dist/
Tecniapp web/.env
Tecniapp web/.firebase/
Tecniapp web/.idea/

# Logs
*.log

# Keystores de firma — NUNCA deben versionarse (ver .github/workflows/release.yml,
# que los toma de Secrets/CI, no del repo).
*.jks
*.keystore

# Exports de datos reales de Firebase (PII de clientes/usuarios: cédulas, teléfonos,
# nombres, códigos de verificación). NUNCA deben versionarse. Se encontraron 4 archivos
# aquí commiteados con datos reales — remediado el 2026-07-09, ver log.md.
firebase json/
*-export.json

# Vite
Tecniapp web/.vite/